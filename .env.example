# Public OAuth client ID — safe to commit/expose.
# Create a Google Cloud "Web application" OAuth client and put the ID here.
# Authorized JS origins for the client must include:
#   - http://127.0.0.1:5174  (local dev)
#   - https://<your-username>.github.io  (production)
VITE_GOOGLE_CLIENT_ID=
